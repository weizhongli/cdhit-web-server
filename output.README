output directory hold user jobs, it can can be a directory or a symbolic link to another directory. 
We suggest to use symbolic link.

Important: the web server user (e.g. apache) need to have read/write permission to the output director
